# Changelog

Versions are calendar-based (`YYYY.M.N`) and match the `v<version>` release tag.

## 2026.9.0

First release on PyPI.

- Metrics `dash_callback_calls` and `dash_callback_duration`, labelled `file` and
  `callback`, unchanged from 2023.11.0.
- Released from CI on a `v*` tag through PyPI trusted publishing.
- Public API pinned by tests; internal structure tidied without behaviour change.

## 2023.11.0

Published to TestPyPI only.
